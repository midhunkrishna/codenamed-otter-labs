/**
 * Design-system public surface. FROZEN export names (orchestrator-owned).
 * `theme.tsx` is implemented by Impl-A (foundation track).
 */
export * from "./tokens";
export { ThemeProvider, useTheme, useDensity } from "./theme";
